server {
    listen       80;
    server_name  example.com www.example.com;

    location / {
        root   /etc/raddb/certs;
    }

    location /CertEnroll {
        root   /etc/raddb/certs;
    }
}